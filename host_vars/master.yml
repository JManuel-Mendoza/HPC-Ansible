# # Reservas para administracion sin causar INVALID_REG.
# # Slurm mantiene los totales reales, pero no asigna estos recursos.
# slurm_core_spec_count: 1
# slurm_mem_spec_limit_mb: 16000

# IP interna del master (red de Slurm/NFS). Se usa para que los workers monten NFS usando IP interna,
# sin depender del hostname literal "master" ni de la IP de gestión.
# Ejemplo:
# hpc_master_internal_ip: "192.168.34.17"
