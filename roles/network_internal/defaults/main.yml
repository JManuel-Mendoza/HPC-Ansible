---
# Defaults intencionalmente vacíos (P11b).
#
# Este rol necesita variables lab-specific (NICs/IPs por enlace). Para evitar hardcodes
# dentro del rol y facilitar replicación, define estas variables en:
# - group_vars/all/vars.yml
#
# Variables esperadas (ver `group_vars/all/vars.yml`):
# - network_internal_keep_if
# - network_internal_exclude_ifaces
# - network_internal_exclude_conn_regex
# - network_internal_links

